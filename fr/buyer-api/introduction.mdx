# API Acheteur

> Accès programmatique à votre compte acheteur OpenWrench : bons de travail, actifs, emplacements, factures, entretien préventif, et plus encore.

L'API Acheteur OpenWrench donne aux exploitants d'installations un accès programmatique à tous les éléments côté acheteur de la plateforme : création et suivi des bons de travail, gestion des actifs et des emplacements, examen des soumissions et des factures, suivi de l'entretien préventif et interrogation de votre réseau de fournisseurs.

## URL de base

```text
https://api.useopenwrench.com/api/external
```

Tous les endpoints acheteur se trouvent sous `/v1/buyer/`.

## Authentification

Chaque requête doit contenir **deux en-têtes** : `X-API-KEY` (votre clé d'API, émise par contact acheteur) et `OW-KEY` (le secret partagé OpenWrench émis en même temps). La clé restreint automatiquement chaque requête à votre entreprise. Vous ne voyez jamais que vos propres données. Les requêtes auxquelles il manque l'un des deux en-têtes renvoient `401`.

```bash
curl -H "X-API-KEY: <your-key>" -H "OW-KEY: <shared-secret>" \
  "https://api.useopenwrench.com/api/external/v1/buyer/ping"
```

Pour obtenir une clé d'API et un secret partagé, contactez [support@useopenwrench.com](mailto:support@useopenwrench.com). Utilisez `GET /v1/buyer/me` pour inspecter l'identité (contact, établissement, entreprise) associée à votre clé.

<Note>
La section **API Internal Teams** plus bas dans cet onglet utilise une **clé d'API partenaire distincte**. Votre clé Acheteur ne pourra pas s'authentifier auprès de ces endpoints `/v1/partners/`. Consultez l'[introduction de l'API Internal Teams](/partners-api/introduction) pour plus de détails.
</Note>

## Limites de débit

10 requêtes par fenêtre de 20 secondes par clé. Au-delà, vous recevrez `429 Too Many Requests`. Attendez, puis réessayez.

## Enveloppe de réponse

Réponses pour une entité unique :

```json
{ "type": "WorkOrder", "data": { "...": "..." }, "status": "ok" }
```

Les réponses de liste ajoutent un `count` total :

```json
{ "type": "WorkOrder", "data": [ "..." ], "count": 42, "status": "ok" }
```

Erreurs :

```json
{ "message": "Human-readable message", "type": "NotFoundException", "status": "error", "traceId": "abc123def45" }
```

`401` signifie une clé manquante ou invalide ; `400` couvre les données saisies erronées, les filtres invalides et les refus de permission ; `429` correspond à la limite de débit.

## Pagination et filtrage

Les endpoints de liste acceptent `offset`, `limit` (par défaut 10, max 25), `sort_by` et `order` (`asc` | `desc`). Les paramètres de requête supplémentaires sont traités comme des filtres de champ. Passez un nom de champ avec une valeur (séparez plusieurs valeurs par des virgules) pour filtrer le jeu de résultats. La page de référence de chaque endpoint énumère ses filtres notables.

## Formats de date

La plupart des horodatages sont des chaînes ISO 8601 avec décalage (par exemple `2026-08-14T13:05:22.000-07:00`) ; certains champs d'horodatage de base de données sont sérialisés sous la forme `yyyy-MM-dd HH:mm:ss.S`. Les dates simples sont au format `yyyy-MM-dd`. Lorsque vous envoyez des dates-heure, utilisez ISO 8601.

## Guides détaillés

Les guides de cet onglet parcourent chaque partie de l'API en profondeur, avec les payloads, les modèles de statut et les schémas d'intégration :

<CardGroup cols={2}>
  <Card title="Bons de travail" href="/buyer-api/work-orders" icon="clipboard-list">
    Création, filtrage, réaffectation, clôture. Modèle de statut, notes et types de problème.
  </Card>
  <Card title="Visites de service" href="/buyer-api/service-calls" icon="user-check">
    Preuves des visites : journaux de travail, temps de travail réel et détails des techniciens.
  </Card>
  <Card title="Actifs et emplacements" href="/buyer-api/assets-and-locations" icon="warehouse">
    Emplacements, régions, types d'actifs, modèles, compteurs et suivi des fluides frigorigènes.
  </Card>
  <Card title="Factures" href="/buyer-api/invoices" icon="file-invoice-dollar">
    Le pipeline d'approbation, la synchronisation avec la comptabilité fournisseurs, les exports aplatis et les mises à jour groupées.
  </Card>
  <Card title="Soumissions et propositions" href="/buyer-api/quotes-and-proposals" icon="file-signature">
    Lisez les soumissions des fournisseurs et rapprochez-les des factures.
  </Card>
  <Card title="Entretien préventif" href="/buyer-api/planned-maintenance" icon="calendar-check">
    Lisez les horaires, sautez des exécutions et pilotez l'entretien préventif depuis un planificateur externe.
  </Card>
  <Card title="Réseau de fournisseurs" href="/buyer-api/supplier-network" icon="network-wired">
    Interrogez votre réseau et classez les fournisseurs du réseau privé pour la répartition.
  </Card>
  <Card title="Visites de site" href="/buyer-api/site-survey-walkthroughs" icon="clipboard-check">
    Visites de site et bons de travail générés à partir de leurs constats.
  </Card>
  <Card title="Fichiers et pièces jointes" href="/buyer-api/files-and-attachments" icon="paperclip">
    Téléversez une seule fois, référencez partout, téléchargez les preuves.
  </Card>
  <Card title="Compte et utilitaires" href="/buyer-api/account-and-utilities" icon="id-badge">
    Ping, identité de la clé, provisionnement des utilisateurs et taux de change.
  </Card>
</CardGroup>

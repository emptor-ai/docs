---
title: "Buyer API errors, status codes, and rate limits"
description: "Understand the Buyer API error envelope, the meaning of 400, 401, 429, and 5xx responses, and how to back off when rate limits are exceeded."
---

## Error envelope

Errors follow the standard envelope:

```json theme={null}
{
  "message": "Human-readable message",
  "type": "NotFoundException",
  "status": "error",
  "traceId": "abc123def45"
}
```

Include the `traceId` when you contact support. It lets us find the exact request in our logs.

## Status codes

- `400` — bad input, bad filter, or permission denied.
- `401` — missing or invalid `X-API-KEY` / `OW-KEY`.
- `429` — rate limit exceeded.
- `5xx` — server error; retry with backoff.

## Rate limits

Every key is capped at **10 requests per 20-second window**. Beyond that you receive `429 Too Many Requests`.

Recommended handling:

1. Catch `429`.
2. Wait at least 20 seconds before retrying.
3. Space background jobs (e.g. full paginated exports) so they stay under the cap.

## Common failure modes

- **Filtering on an unknown field** returns `400`, not an empty list. Double-check the field name against the endpoint's reference page.
- **Requesting a resource outside your scope** returns `400` with a permission message, not `404`.
- **Sending a plain date where an ISO 8601 datetime is expected** returns `400`. See date-format notes on the [reference introduction](/buyer-api/introduction).

# Webhooks

> Receive real-time updates instead of polling

<Info>
  This page is a scaffold. Fill in your actual webhook configuration UI, event types, signing scheme, and retry policy.
</Info>

## Configuring webhooks

Describe here how a buyer configures webhook endpoints (dashboard URL, per-event-type opt-in, staging vs. production, etc.).

## Event types

List the events you emit. For example: work order status changes, quote submitted, invoice submitted.

## Payload shape

Show a representative payload with headers, signature, and body.

## Verifying signatures

Explain the HMAC signing scheme and how consumers should verify a payload came from OpenWrench.

## Retries and idempotency

Document retry cadence, maximum attempts, and the idempotency key consumers can use to deduplicate.

## Handling failures

Recommend a response contract (return `2xx` quickly, do the work asynchronously) and how to recover from missed events.
